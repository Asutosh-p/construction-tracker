// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  BuildingTypeType,
  BuildingTypeCreateInput,
  BuildingTypeUpdateInput,
  BuildingType,
  BuildingCreateInput,
  BuildingUpdateInput,
  FloorType,
  FloorCreateInput,
  FloorUpdateInput,
  RoomType,
  RoomCreateInput,
  RoomUpdateInput,
  ItemTypeType,
  ItemTypeCreateInput,
  ItemTypeUpdateInput,
  InstallationItemType,
  InstallationItemCreateInput,
  InstallationItemUpdateInput,
  WorkflowStepType,
  WorkflowStepCreateInput,
  WorkflowStepUpdateInput,
  WorkflowTemplateType,
  WorkflowTemplateCreateInput,
  WorkflowTemplateUpdateInput,
  WorkflowTemplateStepType,
  WorkflowTemplateStepCreateInput,
  WorkflowTemplateStepUpdateInput,
  RoomItemTemplateType,
  RoomItemTemplateCreateInput,
  RoomItemTemplateUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// BuildingType Client Functions
// ============================================================================

/**
 * List all BuildingType records
 */
export async function getBuildingTypeList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<BuildingTypeType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/building-types${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list BuildingType')
  }
  const json = await response.json()
  return (json.items || []) as BuildingTypeType[]
}

/**
 * Get a single BuildingType by ID
 */
export async function getBuildingTypeById(args: { data: { id: string; userId?: string } }): Promise<BuildingTypeType> {
  const url = `${getApiBase()}/api/building-types/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'BuildingType not found')
  }
  const json = await response.json()
  return json.data as BuildingTypeType
}

/**
 * Create a new BuildingType
 */
export async function createBuildingType(args: { data: { input: BuildingTypeCreateInput; userId?: string } }): Promise<BuildingTypeType> {
  const url = `${getApiBase()}/api/building-types`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create BuildingType')
  }
  const json = await response.json()
  return json.data as BuildingTypeType
}

/**
 * Update an existing BuildingType
 */
export async function updateBuildingType(args: { data: { id: string; input: BuildingTypeUpdateInput; userId?: string } }): Promise<BuildingTypeType> {
  const url = `${getApiBase()}/api/building-types/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update BuildingType')
  }
  const json = await response.json()
  return json.data as BuildingTypeType
}

/**
 * Delete a BuildingType
 */
export async function deleteBuildingType(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/building-types/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete BuildingType')
  }
  return { success: true }
}

// ============================================================================
// Building Client Functions
// ============================================================================

/**
 * List all Building records
 */
export async function getBuildingList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<BuildingType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/buildings${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Building')
  }
  const json = await response.json()
  return (json.items || []) as BuildingType[]
}

/**
 * Get a single Building by ID
 */
export async function getBuildingById(args: { data: { id: string; userId?: string } }): Promise<BuildingType> {
  const url = `${getApiBase()}/api/buildings/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Building not found')
  }
  const json = await response.json()
  return json.data as BuildingType
}

/**
 * Create a new Building
 */
export async function createBuilding(args: { data: { input: BuildingCreateInput; userId?: string } }): Promise<BuildingType> {
  const url = `${getApiBase()}/api/buildings`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Building')
  }
  const json = await response.json()
  return json.data as BuildingType
}

/**
 * Update an existing Building
 */
export async function updateBuilding(args: { data: { id: string; input: BuildingUpdateInput; userId?: string } }): Promise<BuildingType> {
  const url = `${getApiBase()}/api/buildings/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Building')
  }
  const json = await response.json()
  return json.data as BuildingType
}

/**
 * Delete a Building
 */
export async function deleteBuilding(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/buildings/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Building')
  }
  return { success: true }
}

// ============================================================================
// Floor Client Functions
// ============================================================================

/**
 * List all Floor records
 */
export async function getFloorList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<FloorType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/floors${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Floor')
  }
  const json = await response.json()
  return (json.items || []) as FloorType[]
}

/**
 * Get a single Floor by ID
 */
export async function getFloorById(args: { data: { id: string; userId?: string } }): Promise<FloorType> {
  const url = `${getApiBase()}/api/floors/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Floor not found')
  }
  const json = await response.json()
  return json.data as FloorType
}

/**
 * Create a new Floor
 */
export async function createFloor(args: { data: { input: FloorCreateInput; userId?: string } }): Promise<FloorType> {
  const url = `${getApiBase()}/api/floors`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Floor')
  }
  const json = await response.json()
  return json.data as FloorType
}

/**
 * Update an existing Floor
 */
export async function updateFloor(args: { data: { id: string; input: FloorUpdateInput; userId?: string } }): Promise<FloorType> {
  const url = `${getApiBase()}/api/floors/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Floor')
  }
  const json = await response.json()
  return json.data as FloorType
}

/**
 * Delete a Floor
 */
export async function deleteFloor(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/floors/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Floor')
  }
  return { success: true }
}

// ============================================================================
// Room Client Functions
// ============================================================================

/**
 * List all Room records
 */
export async function getRoomList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<RoomType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/rooms${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Room')
  }
  const json = await response.json()
  return (json.items || []) as RoomType[]
}

/**
 * Get a single Room by ID
 */
export async function getRoomById(args: { data: { id: string; userId?: string } }): Promise<RoomType> {
  const url = `${getApiBase()}/api/rooms/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Room not found')
  }
  const json = await response.json()
  return json.data as RoomType
}

/**
 * Create a new Room
 */
export async function createRoom(args: { data: { input: RoomCreateInput; userId?: string } }): Promise<RoomType> {
  const url = `${getApiBase()}/api/rooms`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Room')
  }
  const json = await response.json()
  return json.data as RoomType
}

/**
 * Update an existing Room
 */
export async function updateRoom(args: { data: { id: string; input: RoomUpdateInput; userId?: string } }): Promise<RoomType> {
  const url = `${getApiBase()}/api/rooms/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Room')
  }
  const json = await response.json()
  return json.data as RoomType
}

/**
 * Delete a Room
 */
export async function deleteRoom(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/rooms/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Room')
  }
  return { success: true }
}

// ============================================================================
// ItemType Client Functions
// ============================================================================

/**
 * List all ItemType records
 */
export async function getItemTypeList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ItemTypeType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/item-types${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ItemType')
  }
  const json = await response.json()
  return (json.items || []) as ItemTypeType[]
}

/**
 * Get a single ItemType by ID
 */
export async function getItemTypeById(args: { data: { id: string; userId?: string } }): Promise<ItemTypeType> {
  const url = `${getApiBase()}/api/item-types/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ItemType not found')
  }
  const json = await response.json()
  return json.data as ItemTypeType
}

/**
 * Create a new ItemType
 */
export async function createItemType(args: { data: { input: ItemTypeCreateInput; userId?: string } }): Promise<ItemTypeType> {
  const url = `${getApiBase()}/api/item-types`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ItemType')
  }
  const json = await response.json()
  return json.data as ItemTypeType
}

/**
 * Update an existing ItemType
 */
export async function updateItemType(args: { data: { id: string; input: ItemTypeUpdateInput; userId?: string } }): Promise<ItemTypeType> {
  const url = `${getApiBase()}/api/item-types/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ItemType')
  }
  const json = await response.json()
  return json.data as ItemTypeType
}

/**
 * Delete a ItemType
 */
export async function deleteItemType(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/item-types/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ItemType')
  }
  return { success: true }
}

// ============================================================================
// InstallationItem Client Functions
// ============================================================================

/**
 * List all InstallationItem records
 */
export async function getInstallationItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<InstallationItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/installation-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list InstallationItem')
  }
  const json = await response.json()
  return (json.items || []) as InstallationItemType[]
}

/**
 * Get a single InstallationItem by ID
 */
export async function getInstallationItemById(args: { data: { id: string; userId?: string } }): Promise<InstallationItemType> {
  const url = `${getApiBase()}/api/installation-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'InstallationItem not found')
  }
  const json = await response.json()
  return json.data as InstallationItemType
}

/**
 * Create a new InstallationItem
 */
export async function createInstallationItem(args: { data: { input: InstallationItemCreateInput; userId?: string } }): Promise<InstallationItemType> {
  const url = `${getApiBase()}/api/installation-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create InstallationItem')
  }
  const json = await response.json()
  return json.data as InstallationItemType
}

/**
 * Update an existing InstallationItem
 */
export async function updateInstallationItem(args: { data: { id: string; input: InstallationItemUpdateInput; userId?: string } }): Promise<InstallationItemType> {
  const url = `${getApiBase()}/api/installation-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update InstallationItem')
  }
  const json = await response.json()
  return json.data as InstallationItemType
}

/**
 * Delete a InstallationItem
 */
export async function deleteInstallationItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/installation-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete InstallationItem')
  }
  return { success: true }
}

// ============================================================================
// WorkflowStep Client Functions
// ============================================================================

/**
 * List all WorkflowStep records
 */
export async function getWorkflowStepList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WorkflowStepType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/workflow-steps${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list WorkflowStep')
  }
  const json = await response.json()
  return (json.items || []) as WorkflowStepType[]
}

/**
 * Get a single WorkflowStep by ID
 */
export async function getWorkflowStepById(args: { data: { id: string; userId?: string } }): Promise<WorkflowStepType> {
  const url = `${getApiBase()}/api/workflow-steps/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'WorkflowStep not found')
  }
  const json = await response.json()
  return json.data as WorkflowStepType
}

/**
 * Create a new WorkflowStep
 */
export async function createWorkflowStep(args: { data: { input: WorkflowStepCreateInput; userId?: string } }): Promise<WorkflowStepType> {
  const url = `${getApiBase()}/api/workflow-steps`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create WorkflowStep')
  }
  const json = await response.json()
  return json.data as WorkflowStepType
}

/**
 * Update an existing WorkflowStep
 */
export async function updateWorkflowStep(args: { data: { id: string; input: WorkflowStepUpdateInput; userId?: string } }): Promise<WorkflowStepType> {
  const url = `${getApiBase()}/api/workflow-steps/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update WorkflowStep')
  }
  const json = await response.json()
  return json.data as WorkflowStepType
}

/**
 * Delete a WorkflowStep
 */
export async function deleteWorkflowStep(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/workflow-steps/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete WorkflowStep')
  }
  return { success: true }
}

// ============================================================================
// WorkflowTemplate Client Functions
// ============================================================================

/**
 * List all WorkflowTemplate records
 */
export async function getWorkflowTemplateList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WorkflowTemplateType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/workflow-templates${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list WorkflowTemplate')
  }
  const json = await response.json()
  return (json.items || []) as WorkflowTemplateType[]
}

/**
 * Get a single WorkflowTemplate by ID
 */
export async function getWorkflowTemplateById(args: { data: { id: string; userId?: string } }): Promise<WorkflowTemplateType> {
  const url = `${getApiBase()}/api/workflow-templates/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'WorkflowTemplate not found')
  }
  const json = await response.json()
  return json.data as WorkflowTemplateType
}

/**
 * Create a new WorkflowTemplate
 */
export async function createWorkflowTemplate(args: { data: { input: WorkflowTemplateCreateInput; userId?: string } }): Promise<WorkflowTemplateType> {
  const url = `${getApiBase()}/api/workflow-templates`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create WorkflowTemplate')
  }
  const json = await response.json()
  return json.data as WorkflowTemplateType
}

/**
 * Update an existing WorkflowTemplate
 */
export async function updateWorkflowTemplate(args: { data: { id: string; input: WorkflowTemplateUpdateInput; userId?: string } }): Promise<WorkflowTemplateType> {
  const url = `${getApiBase()}/api/workflow-templates/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update WorkflowTemplate')
  }
  const json = await response.json()
  return json.data as WorkflowTemplateType
}

/**
 * Delete a WorkflowTemplate
 */
export async function deleteWorkflowTemplate(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/workflow-templates/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete WorkflowTemplate')
  }
  return { success: true }
}

// ============================================================================
// WorkflowTemplateStep Client Functions
// ============================================================================

/**
 * List all WorkflowTemplateStep records
 */
export async function getWorkflowTemplateStepList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WorkflowTemplateStepType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/workflow-template-steps${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list WorkflowTemplateStep')
  }
  const json = await response.json()
  return (json.items || []) as WorkflowTemplateStepType[]
}

/**
 * Get a single WorkflowTemplateStep by ID
 */
export async function getWorkflowTemplateStepById(args: { data: { id: string; userId?: string } }): Promise<WorkflowTemplateStepType> {
  const url = `${getApiBase()}/api/workflow-template-steps/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'WorkflowTemplateStep not found')
  }
  const json = await response.json()
  return json.data as WorkflowTemplateStepType
}

/**
 * Create a new WorkflowTemplateStep
 */
export async function createWorkflowTemplateStep(args: { data: { input: WorkflowTemplateStepCreateInput; userId?: string } }): Promise<WorkflowTemplateStepType> {
  const url = `${getApiBase()}/api/workflow-template-steps`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create WorkflowTemplateStep')
  }
  const json = await response.json()
  return json.data as WorkflowTemplateStepType
}

/**
 * Update an existing WorkflowTemplateStep
 */
export async function updateWorkflowTemplateStep(args: { data: { id: string; input: WorkflowTemplateStepUpdateInput; userId?: string } }): Promise<WorkflowTemplateStepType> {
  const url = `${getApiBase()}/api/workflow-template-steps/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update WorkflowTemplateStep')
  }
  const json = await response.json()
  return json.data as WorkflowTemplateStepType
}

/**
 * Delete a WorkflowTemplateStep
 */
export async function deleteWorkflowTemplateStep(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/workflow-template-steps/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete WorkflowTemplateStep')
  }
  return { success: true }
}

// ============================================================================
// RoomItemTemplate Client Functions
// ============================================================================

/**
 * List all RoomItemTemplate records
 */
export async function getRoomItemTemplateList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<RoomItemTemplateType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/room-item-templates${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list RoomItemTemplate')
  }
  const json = await response.json()
  return (json.items || []) as RoomItemTemplateType[]
}

/**
 * Get a single RoomItemTemplate by ID
 */
export async function getRoomItemTemplateById(args: { data: { id: string; userId?: string } }): Promise<RoomItemTemplateType> {
  const url = `${getApiBase()}/api/room-item-templates/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'RoomItemTemplate not found')
  }
  const json = await response.json()
  return json.data as RoomItemTemplateType
}

/**
 * Create a new RoomItemTemplate
 */
export async function createRoomItemTemplate(args: { data: { input: RoomItemTemplateCreateInput; userId?: string } }): Promise<RoomItemTemplateType> {
  const url = `${getApiBase()}/api/room-item-templates`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create RoomItemTemplate')
  }
  const json = await response.json()
  return json.data as RoomItemTemplateType
}

/**
 * Update an existing RoomItemTemplate
 */
export async function updateRoomItemTemplate(args: { data: { id: string; input: RoomItemTemplateUpdateInput; userId?: string } }): Promise<RoomItemTemplateType> {
  const url = `${getApiBase()}/api/room-item-templates/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update RoomItemTemplate')
  }
  const json = await response.json()
  return json.data as RoomItemTemplateType
}

/**
 * Delete a RoomItemTemplate
 */
export async function deleteRoomItemTemplate(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/room-item-templates/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete RoomItemTemplate')
  }
  return { success: true }
}
